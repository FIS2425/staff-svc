apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mongo-data-staff
  name: mongo-data-staff
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
